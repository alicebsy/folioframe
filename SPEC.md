# 직군 맞춤형 통합 웹 포트폴리오 — SPEC

## 1. 목표

취업 준비생이 프로젝트 경험을 **담당 역할 → 문제 상황 → 해결 과정 → 구체적 성과** 순서로 정리하고, 이미지·문서·링크와 함께 하나의 웹 포트폴리오로 발행할 수 있게 한다.

채용 담당자는 공유 URL을 통해 로그인 없이 공개 포트폴리오를 볼 수 있다.

---

## 2. 핵심 기능

1. 역할·문제·해결 과정·성과 기반 프로젝트 작성
2. 이미지·PDF·링크 등록 및 GitHub 공개 레포지토리 가져오기
3. 프로젝트 정렬·공개 설정 및 고유 URL 발행

---

## 3. 화면

MVP는 다음 2개 화면으로 구성한다.

### 편집 대시보드

```text
+------------------------------------------------------------+
| Portfolio Hub                    [미리보기] [발행하기]      |
+------------------------------------------------------------+
| 홍길동 · 프로덕트 매니저                  [프로필 편집]   |
+------------------------------------------------------------+
| 프로젝트                     [+ 직접 등록] [GitHub 가져오기] |
+------------------------------------------------------------+
| :: 프로젝트 A                         [공개 ▼] [수정]       |
|    역할 · 문제 · 해결 과정 · 성과 요약                      |
+------------------------------------------------------------+
| :: 프로젝트 B                       [비공개 ▼] [수정]       |
|    GitHub에서 가져온 초안 · 필수 항목 작성 필요             |
+------------------------------------------------------------+
```

### 공개 포트폴리오

- 프로필: 이름, 희망 직무, 한 줄 소개, 공개 연락처
- 공개 프로젝트만 사용자가 정한 순서로 표시
- 각 프로젝트에 개요, 역할, 문제, 해결 과정, 성과 표시
- 등록된 이미지, PDF, 외부 링크 표시

---

## 4. 입력값

### 프로필

| 항목 | 필수 | 제한 |
|---|---:|---|
| 이름 | O | 30자 |
| 희망 직무 | O | 50자 |
| 한 줄 소개 | O | 160자 |
| 프로필 이미지 | X | JPG, PNG, WEBP |
| 공개 이메일 | X | 이메일 형식 |
| 외부 링크 | X | 최대 3개 |

### 프로젝트

| 항목 | 필수 | 제한 |
|---|---:|---|
| 프로젝트명 | O | 60자 |
| 프로젝트 개요 | O | 500자 |
| 담당 역할 | O | 500자 |
| 문제 상황 | O | 500자 |
| 해결 과정 | O | 1,000자 |
| 구체적 성과 | O | 500자 |
| 기간 | X | 시작일, 종료일 또는 진행 중 |
| 대표·상세 이미지 | X | 최대 6장, 파일당 5MB |
| PDF 문서 | X | 최대 3개, 파일당 10MB |
| 관련 링크 | X | 최대 5개 |
| 기술 스택 | X | 최대 10개 |

각 필수 항목에는 다음 작성 가이드를 표시한다.

- 담당 역할: 본인이 책임지고 직접 수행한 일은 무엇인가?
- 문제 상황: 해결해야 했던 문제와 제약 조건은 무엇인가?
- 해결 과정: 무엇을 시도했고 어떤 근거로 해결 방법을 선택했는가?
- 구체적 성과: 해결 후 확인된 수치나 변화는 무엇인가?

---

## 5. 주요 동작

### 프로젝트 직접 등록

1. **[+ 직접 등록]**을 누른다.
2. 프로젝트 필수 항목과 필요한 자료를 입력한다.
3. **[저장]**을 누르면 비공개 프로젝트로 추가된다.

필수값이 비어 있으면 저장할 수 없다.

### GitHub에서 가져오기

1. **[GitHub 가져오기]**를 누르고 GitHub 계정을 연결한다.
2. 공개 레포지토리 하나를 선택한다.
3. 레포지토리명, Description 또는 README, 주 언어, URL을 프로젝트 초안으로 가져온다.
4. 사용자가 역할, 문제, 해결 과정, 성과를 직접 작성한다.

가져온 항목은 필수값을 모두 작성하기 전까지 비공개 초안으로 유지한다.

### 정렬 및 공개

- 프로젝트 카드를 드래그하여 순서를 변경한다.
- 작성이 완료된 프로젝트만 공개로 전환할 수 있다.
- 공개 포트폴리오에는 공개 프로젝트만 설정한 순서대로 표시한다.

### 발행

1. 공개 프로젝트를 1개 이상 설정한다.
2. 미리보기에서 결과를 확인한다.
3. **[발행하기]**를 눌러 고유 URL을 생성한다.
4. 발행 후 프로젝트 수정·정렬·공개 상태 변경은 공개 화면에도 반영한다.

---

## 6. 데이터 모델

### Profile

```ts
type Profile = {
  userId: string
  name: string
  jobTitle: string
  bio: string
  profileImageUrl?: string
  contactEmail?: string
  externalLinks: { label: string; url: string }[]
  portfolioSlug?: string
  isPublished: boolean
}
```

### Project

```ts
type Project = {
  id: string
  userId: string
  source: "manual" | "github"
  contentStatus: "draft" | "complete"

  title: string
  summary: string
  role?: string
  problem?: string
  troubleshooting?: string
  result?: string

  startDate?: string
  endDate?: string
  isOngoing: boolean
  imageUrls: string[]
  documentUrls: string[]
  links: { label: string; url: string }[]
  techStacks: string[]
  githubRepositoryUrl?: string

  displayOrder: number
  isPublic: boolean
}
```

### 데이터 규칙

- 사용자는 자신의 데이터만 수정할 수 있다.
- 역할·문제·해결 과정·성과가 모두 있으면 `complete`로 처리한다.
- `complete` 프로젝트만 공개할 수 있다.
- 공개 API는 비공개 프로젝트를 반환하지 않는다.
- `portfolioSlug`는 중복될 수 없다.

---

## 7. API

| Method | Endpoint | 설명 |
|---|---|---|
| `GET` | `/api/profile` | 내 프로필 조회 |
| `PUT` | `/api/profile` | 프로필 저장 |
| `GET` | `/api/projects` | 내 프로젝트 목록 |
| `POST` | `/api/projects` | 프로젝트 등록 |
| `PUT` | `/api/projects/:id` | 프로젝트 수정 |
| `PATCH` | `/api/projects/reorder` | 프로젝트 순서 변경 |
| `PATCH` | `/api/projects/:id/visibility` | 공개 상태 변경 |
| `GET` | `/api/github/repositories` | 공개 레포지토리 목록 |
| `POST` | `/api/github/repositories/:id/import` | 레포지토리 가져오기 |
| `POST` | `/api/uploads` | 이미지 또는 PDF 업로드 |
| `POST` | `/api/portfolio/publish` | 포트폴리오 발행 |
| `GET` | `/api/public/:slug` | 공개 포트폴리오 조회 |

---

## 8. 제외 범위

- AI 콘텐츠 생성
- 자유로운 디자인 편집기와 다수의 테마
- 커스텀 도메인
- 이력서·자기소개서 생성
- GitHub 외 서비스 연동
- 비공개 GitHub 레포지토리와 자동 재동기화
- 공동 편집, 댓글, 방문자 분석
- 채용 공고·지원 현황·지원자 평가 기능
- PDF 포트폴리오 내보내기

---

## 9. 완료 조건

- 프로필을 저장하고 다시 조회할 수 있다.
- 필수 구조에 맞춰 프로젝트를 등록·수정할 수 있다.
- 이미지, PDF, 링크를 프로젝트에 추가할 수 있다.
- GitHub 공개 레포지토리를 비공개 초안으로 가져올 수 있다.
- 프로젝트 순서와 공개 상태를 변경할 수 있다.
- 미완성 프로젝트는 공개할 수 없다.
- 공개 프로젝트가 1개 이상이면 포트폴리오를 발행할 수 있다.
- 고유 URL에서 공개 프로젝트만 로그인 없이 볼 수 있다.
- 새로고침 후에도 데이터가 유지된다.
